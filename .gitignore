# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
.DS_Store
dist
dev-dist
dist-ssr
# 只忽略根目录的测试覆盖率产物。
# 裸写 coverage 会匹配**任意层级**的同名目录 —— 它曾把 src/views/perf/coverage/
# 整个源码目录忽略掉，导致「覆盖率看板」「覆盖率明细」两个页面从未进入仓库，
# 菜单指向的组件在别人 clone 后并不存在。
/coverage
*.local

/cypress/videos/
/cypress/screenshots/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

*.tsbuildinfo

# lock files (use pnpm-lock.yaml only)
package-lock.json
yarn.lock

# Vite temp files
*.timestamp-*.mjs

# Local dev process state
.run/
